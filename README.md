# Hadoop Single & Multi Nodes Implementation

Implementation of Hadoop MapReduce on single node (Docker) and instructions of applying MapReduce in a multi-node Hadoop environment (Kubernetes + Helm).
